{% extends 'BoundBackOfficeBundle::layout.html.twig' %}

{% block body %}
    {% for achievement in achievements %}
        <pre>
            {{ achievement.title }}
            {{ achievement.content }}
            {{ achievement.points }}
            {{ achievement.type }}
        </pre>
    {% endfor %}
{% endblock %}
